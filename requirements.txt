# ── Python 3.10.x 전용 환경에서 설치하세요 ──────────────────
# 가상환경 생성: py -3.10 -m venv venv
# 활성화:        venv\Scripts\activate
# 설치:          pip install -r requirements.txt

# ── GPU (CUDA) PyTorch ────────────────────────────────────────
# CUDA 버전 확인 후 아래 중 하나만 주석 해제하여 설치하세요
# nvidia-smi 명령어로 CUDA Version 확인
#
# CUDA 12.1:
#   pip install torch==2.1.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# CUDA 11.8:
#   pip install torch==2.1.0+cu118 --index-url https://download.pytorch.org/whl/cu118

# ── ABSA 분석 (absa_scoring.py) ───────────────────────────────
pyabsa==2.4.3
numpy==1.24.4               # pyabsa 호환 버전 고정 (1.25 이상 사용 불가)
pandas>=1.5.0
tqdm>=4.64.0

# ── 파일 입출력 ───────────────────────────────────────────────
pyarrow>=10.0.0              # parquet 파일 읽기/쓰기

# ── LLM 이슈 탐지 파이프라인 (issue_pipeline.py, retry_failed.py) ──
requests>=2.28.0             # Ollama API 호출
scikit-learn>=1.2.0          # TF-IDF, K-Means 클러스터링

# ── 대시보드 (issue_dashboard.py) ────────────────────────────
streamlit>=1.28.0
plotly>=5.15.0
vaderSentiment>=3.3.2        # VADER 감성 분석
deep-translator>=1.11.4      # 가장 시급한 이슈 한/영 번역 토글

# ── Git (코드 실행 전 별도 설치 필요) ────────────────────────
# pyabsa 내부에서 gitpython을 사용하므로 Git 설치 필수
# 다운로드: https://git-scm.com/download/win
# 설치 후 VS Code 재시작 필요

# ── AI 이슈 진단 (Dashboard.py) ───────────────────────────────
openai>=1.0.0                # GPT-5.4-mini API 호출


# ────────────────────────────────────────────────────────────
py -3.10 -m venv venv
venv\Scripts\activate

streamlit run Dashboard.py